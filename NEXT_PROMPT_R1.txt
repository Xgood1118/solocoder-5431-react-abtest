深挖还有 5 个坑：分桶直接 Math.random 没用 visitorId 哈希，清缓存就换桶；checkLayerAssignment 永远 return true 是空函数；geolocation 规则 default 直接 pass，配北京等于全开；变体编辑器连续 onChange 第二次拿到的是旧 html；iframe sandbox 加了 allow-same-origin 反而放开父页面访问。检查修复一下。